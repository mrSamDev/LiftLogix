{
  "name": "api",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch index.ts",
    "build": "bun build index.ts --outdir ./dist --target bun",
    "start": "bun run index.ts",
    "clean": "rm -rf dist",
    "seed": "bun run seed.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@lift-logic/types": "workspace:*",
    "better-auth": "^1.4.10",
    "hono": "^4.11.3",
    "mongodb": "^7.0.0",
    "mongoose": "^9.1.1"
  }
}
